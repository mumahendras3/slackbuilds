# Config file to be used:
CONF=/etc/monerod.conf

# PID file location:
PID_FILE=/run/monero/monerod.pid

# Uncomment below to run monerod through Tor:
#export DNS_PUBLIC=tcp

# The following is needed only when accessing wallet from a different host in
# the LAN, VPN, etc, the RPC must bind to 0.0.0.0, but by default torsocks only
# allows binding to localhost.
#export TORSOCKS_ALLOW_INBOUND=1
